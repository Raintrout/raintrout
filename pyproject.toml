[project]
name = "raintrout-profile"
version = "0.1.0"
description = "Generate the profile README SVG assets."
requires-python = ">=3.12"
dependencies = [
    "astropy==7.0.1",
    "astroquery==0.4.10",
    "Mako==1.3.12",
    "numpy==2.2.4",
    "PyYAML==6.0.2",
]

[tool.uv]
package = false
